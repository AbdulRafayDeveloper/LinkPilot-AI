/**
 * The About Me tab shared by the outreach modules (First Message and InMail). Each module
 * has its own tone list (constants/firstMessage.ts, constants/inmail.ts); the sender
 * profile (services/senderProfile.ts) is edited as an extra tab next to those prompts.
 */
export const ABOUT_ME_TAB_ID = "about-me"
export const ABOUT_ME_TAB_LABEL = "About Me (sender)"
